# repo init

**Date:** 2026-05-13
**Time:** 22:01
**Scope:** Repository initialization
**Repository:** NLPCC2026-Shared-Task-4

## Summary of Changes

Initialized the repository for clean S0-S4 development without modifying the official starter kit under `NLPCC_tasks/`. Added root agent guidance, command reference, cleaned strategy summary, custom package skeleton, placeholder configs, canonical implementation-log tooling, report directories, and a stricter artifact ignore policy.

No full trading strategy, fake backtest result, fake metric, fake LLM output, or submission package was created.

## Read-First Evidence Table

| File | Exists? | Key facts extracted | Implications for init | Action needed |
| --- | --- | --- | --- | --- |
| `README.md` | Yes | Task uses daily Top-20 Financial Hot News and historical price data; agents generate daily ETF target weights; friction is `0.01%`; Track 1 is macro allocation; Track 2 is sector rotation; 2024/2025/2026 are train/Phase A/Phase B. | Preserve competition facts in docs and keep strategy outputs as target weights. | Referenced in `AGENTS.md`, command reference, and strategy summary. |
| `README-CN.md` | Yes | Chinese README exists but displays mojibake in this workspace; visible facts mirror root English README. | Do not rely on unreadable text for new claims. | Record encoding/display issue; no edit. |
| `NLPCC_tasks/README.md` | Yes | Starter kit includes server, public dataset, leakage-safe DataLoader, agent client, demo runner, API guidance; lists track pools, server endpoints, data split, no-future-leakage rules, and demo commands. | Treat `NLPCC_tasks/` as official/vendor code and build custom code separately. | Preserve official files; cite commands in command reference. |
| `NLPCC_tasks/README-CN.md` | Yes | Chinese starter README exists but displays mojibake; visible structure mirrors English starter README. | Use English starter README for reliable extracted facts. | No edit. |
| `docs/design/repo_architecture.md` | Yes | Existing architecture already requires official code under `NLPCC_tasks/`, custom code under `src/nlpcc4/`, target-weight strategy outputs, execution-layer trade conversion, ignored `.var`/outputs/logs/cache/artifacts, configs, scripts, tests, reports, and S1-S4 modules. | Use as governing architecture; add a Phase-0 canonical scope note to avoid deferred-strategy confusion. | Patched top section only. |
| `docs/research/phase0-init.md` | Yes | Long research consolidation says S1 is first core quant strategy; S2/S3 use bounded LLM signals; S4 is Track 2 event overlay; pure LLM allocation, direct trade generation, heavy RAG, and multi-agent methods are rejected/deferred. | Create cleaned implementation-oriented summary rather than another brainstorming doc. | Preserved as source; added `docs/research/strategy_decision_summary.md`. |
| `phase0-init.md` | No | Missing at repo root. | Do not infer root-specific notes. | Recorded missing; no action. |
| `tools/create_impl_log.py` | No | Missing from canonical tools path before init. | Need canonical generator under `tools/`. | Created `tools/create_impl_log.py`. |
| `create_impl_log.py` | No | Missing at repo root. | No root script to move. | No action. |
| `docs/reports/create_impl_log.py` | Yes | Existing generator created timestamped logs but documented/defaulted to `docs/temp`; from this location the old default would resolve incorrectly. | Do not use old default silently. | Added canonical tool and changed this file to a compatibility wrapper. |
| `.gitignore` | Yes | Initially only contained a commented dataset line. | Runtime outputs, secrets, caches, archives, and derived data were not adequately ignored. | Replaced with artifact policy while keeping source/docs/config dirs trackable. |
| `NLPCC_tasks/.gitignore` | Yes | Official starter ignores Python caches, logs, demo outputs, session mappings, news summary cache, server exp logs, and backtest results. | Official starter has local output ignores; root still needed broader policy. | No edit. |
| `NLPCC_tasks/config.py` | Yes | Defines host `0.0.0.0`, port `6207`, data dirs, log dir, default backtest dates, fund pool, news sources, `top_rank`, and `pre_k_days`. | Command reference should use port `6207` and official data paths. | No edit. |
| `NLPCC_tasks/start_server.py` | Yes | Starts FastAPI via uvicorn and initializes official data loader; file displays mojibake but functional structure is visible. | Start command should stay inside `NLPCC_tasks/`; no custom server wrapper yet. | No edit. |
| `NLPCC_tasks/server_platform/app/core/data_loader.py` | Yes | Provides trading dates, price access, historical prices, benchmark data, and news. Historical prices expose previous full days plus current open with current close/high/low hidden. News is date-windowed and same-day items are filtered to before 15:00, though the docstring also says current-day news should not be included. | Strategy code must reuse/validate this leakage-safe behavior before implementing features. | No edit; discrepancy recorded under issues. |
| `NLPCC_tasks/server_platform/app/core/backtest.py` | Yes | Backtest stores holdings as monetary value, updates holdings by daily pct change, executes buys before sells, charges `0.0001` commission, persists results under `backtest_results*`, and records portfolio snapshots/trades/decisions. | Trade converter must respect official buy/sell semantics and avoid assuming sells fund same-day buys. | No edit. |
| `NLPCC_tasks/server_platform/app/api/backtest.py` | Yes | Exposes start, resume, trade, status, next_day, day_data, news, market_data, historical_prices, decisions, results, visualization, portfolio and holdings endpoints. | Custom clients should target these APIs rather than changing server code. | No edit. |
| `NLPCC_tasks/agent_platform/client/platform_client.py` | Yes | Participant client handles registration/login and wraps start/resume/trade/status/news/market/historical/results APIs. | Future custom runner can reuse API semantics or wrap this client. | No edit. |
| `NLPCC_tasks/agent_platform/demo_backtest.py` | Yes | Demo defines macro and industry fund pools, registers/logs in, starts session, fetches historical prices/news/portfolio, asks advanced LLM agent, submits official trade payloads, and writes demo outputs. | Useful reference, but custom strategies should not emit direct trade payloads. | No edit. |
| `NLPCC_tasks/dataset/README.md` | Yes | Documents price/news data layout, adjusted price behavior, news sources, DataLoader flow, no-leakage historical price behavior, and local `dataloader_eval.py` test command; displays mojibake but key facts are visible. | Command reference should include the DataLoader smoke command. | No edit. |
| `NLPCC_tasks/dataset/dataloader_eval.py` | Yes | Offline DataLoader mirrors server-side trading dates, price loading, historical current-open-only behavior, news filtering, benchmark data, and a local smoke test. | Use for leakage-safe local validation before strategy coding. | No edit. |

## Repository Inspection Result

- Repository root: `D:\Projects\NLPCC2026-Shared-Task-4`.
- Official starter kit exists under `NLPCC_tasks/` with `agent_platform/`, `dataset/`, `server_platform/`, `.env.example`, `.gitignore`, `config.py`, `requirements.txt`, and `start_server.py`.
- Existing local docs before this init included `docs/design/repo_architecture.md`, `docs/research/phase0-init.md`, and `docs/reports/create_impl_log.py`.
- Pre-existing working tree entries not created by this init included `.idea/NLPCC2026-Shared-Task-4.iml`, `.idea/misc.xml`, and `pyproject.toml`.
- No official starter-kit files were modified during this init.

## Files Created

- `AGENTS.md`
- `docs/command_reference.md`
- `docs/research/strategy_decision_summary.md`
- `tools/create_impl_log.py`
- `docs/reports/implementation_logs/20260513_2201_repo_init.md`
- `.gitkeep` files under `docs/reports/implementation_logs/`, `docs/reports/ablations/`, `docs/reports/phase_a/`, `docs/reports/final_submission/`, and `docs/figures/`
- package skeleton under `src/nlpcc4/`
- placeholder configs under `configs/track1/` and `configs/track2/`
- `tests/test_import_contracts.py`

## Files Modified

- `.gitignore`
- `docs/design/repo_architecture.md`
- `docs/reports/create_impl_log.py`

## Files Intentionally Not Touched

- All files under `NLPCC_tasks/`
- `README.md` and `README-CN.md`
- `docs/research/phase0-init.md`
- pre-existing `.idea/*`
- pre-existing `pyproject.toml`

## Verification Commands Run

```powershell
python .\tools\create_impl_log.py "repo init"
python -B -c "import ast, pathlib; paths=list(pathlib.Path('src').rglob('*.py'))+list(pathlib.Path('tools').rglob('*.py'))+list(pathlib.Path('tests').rglob('*.py')); [ast.parse(p.read_text(encoding='utf-8'), filename=str(p)) for p in paths]; print(f'AST syntax check passed for {len(paths)} files')"
$env:PYTHONPATH = (Resolve-Path 'src').Path
python -m pytest -q tests -p no:cacheprovider
git status --short -- NLPCC_tasks
git check-ignore -v .var outputs logs cache artifacts raw_llm_outputs llm_cache prompt_cache references_tmp scratch submissions test.zip test.parquet .env .env.local
git check-ignore -v tmp/x temp/x docs/references_tmp/x test.pickle test.tar
git diff --check
```

## Verification Results

- Implementation log generator created `docs/reports/implementation_logs/20260513_2201_repo_init.md`.
- AST syntax check passed for 19 Python files without writing bytecode.
- Targeted pytest run passed: `2 passed in 0.03s`.
- Final targeted pytest rerun passed: `2 passed in 0.04s`.
- `git diff --check` passed after removing trailing whitespace in the architecture document; only CRLF normalization warnings were reported.
- `git status --short -- NLPCC_tasks` returned no changes.
- Ignore checks confirmed `.var/`, runtime outputs, LLM caches, temp dirs, secrets, archives, and large derived data patterns are ignored.
- `docs/temp` is absent.

## Issues Found

- `python -m compileall -q src tools` failed because the environment denied writes to `__pycache__` bytecode paths. Replaced with AST syntax verification.
- A broad `pytest` invocation passed once but created a `pytest-cache-files-*` temp directory and a later broad collection hit a permission error on that directory. Added `pytest-cache-files-*/` to `.gitignore` and verified with a targeted `tests` run.
- Several official Chinese/Chinese-comment files display mojibake in this workspace. They were not edited.
- `NLPCC_tasks/server_platform/app/core/data_loader.py` has a news docstring saying current-day news should not be included, while the implementation includes same-day news before 15:00. This must be clarified before leakage-sensitive strategy work.
- Official backtest execution processes buy trades before sell trades, so future trade conversion cannot assume same-day sell proceeds fund same-day buys.

## Must Fix Before Real Implementation

- Confirm official news timing semantics: previous-day-only versus same-day-before-15:00.
- Add real dependency management for local custom code if `pip install -e .` becomes part of the workflow.
- Implement S0 baselines before S1 performance claims.
- Implement and test target-weight validation, constraint projection, turnover control, and official trade conversion.
- Add no-leakage tests around DataLoader wrappers before using price/news features.
- Define Track 1 and Track 2 universe sources in custom code without duplicating stale official data.
- Add deterministic run metadata and report generation before producing benchmark tables.

## Self-Review Checklist

- [x] Official starter kit preserved under `NLPCC_tasks/`.
- [x] Custom code separated under `src/nlpcc4/`.
- [x] Strategy contract outputs target weights before trade conversion.
- [x] Runtime artifacts and secrets ignored by Git.
- [x] No full trading strategy, fake backtest, fake metrics, or fake LLM output added.
- [x] Lightweight verification completed.
- [x] Implementation log records missing required files and discrepancies.
- [x] Existing official files were not restructured or moved.

## Final Readiness Status

Ready for the next phase of development: S0 baselines and S1 infrastructure. Do not start real strategy coding until the news timing discrepancy and target-weight-to-trade conversion assumptions are explicitly resolved.
