# docs/prompts/

Prompt archive for this repository.

- `execution/` contains the numbered implementation prompts to be executed in order.
- `reference/` contains earlier research/planning prompts preserved for context.

All prompts must require an implementation log through `create_implementation_log`.
