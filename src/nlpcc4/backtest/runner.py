"""Custom backtest runner entry points.

Future code should wrap the official server/client rather than replacing the
starter-kit backtest engine.
"""


def run_backtest() -> None:
    """Run a custom backtest once integration exists."""
    raise NotImplementedError("Backtest runner is planned.")
