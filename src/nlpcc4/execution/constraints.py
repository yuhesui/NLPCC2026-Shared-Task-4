"""Portfolio constraint projection stubs."""

from typing import Mapping


def project_long_only(weights: Mapping[str, float]) -> dict[str, float]:
    """Return long-only weights once projection rules are finalized."""
    raise NotImplementedError("Constraint projection is planned.")
