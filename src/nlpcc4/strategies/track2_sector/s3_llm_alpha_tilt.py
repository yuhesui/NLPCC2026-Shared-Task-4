"""S3 Track 2 bounded LLM alpha-tilt optimizer.

The LLM may emit bounded JSON alpha scores only; risk controls convert signals
into target-weight tilts around S1.
"""


def build_strategy() -> object:
    """Build the Track 2 S3 strategy once implemented."""
    raise NotImplementedError("Track 2 S3 is planned.")
