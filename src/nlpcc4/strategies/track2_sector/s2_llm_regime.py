"""S2 Track 2 structured LLM sector-regime allocator.

The LLM may emit bounded JSON regime signals only; strategy output remains
target weights with fallback to S1.
"""


def build_strategy() -> object:
    """Build the Track 2 S2 strategy once implemented."""
    raise NotImplementedError("Track 2 S2 is planned.")
